<!--
<config>
    <interface hosts="eNodeB*" names="cellular" address="10.x.x.x" netmask="255.0.0.0"/>
    <interface hosts="ue*" names="cellular" address="10.x.x.x" netmask="255.0.0.0"/>
    <interface hosts="car[*]" names="wlan[0]" address="10.1.1.x" netmask="255.255.255.0"/>

    <interface hosts="*" address="192.168.x.x" netmask="255.255.255.0"/>
</config>
-->

<config>
    <autoConfig>
    	<!-- Configurar interfaz Server -->
    	<interface hosts="server" names="eth[0]" />
    	
        <!-- Configurar interfaz Wi-Fi -->
        <interface hosts="car[*]" names="wlan[0]" />

        <!-- Configurar interfaz celular (LTE/5G) -->
        <interface hosts="car[*]" names="cellularNic" />
    </autoConfig>
</config>